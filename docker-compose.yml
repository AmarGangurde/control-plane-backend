services:
  control-plane:
    build: .
    image: control-plane-backend:latest
    container_name: backend-app
    network_mode: host
    volumes:
      - /home/ubuntu/data:/app/data
      - /home/ubuntu/.kube/config:/root/.kube/config:ro
      - /home/ubuntu/.env:/app/.env:ro
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
